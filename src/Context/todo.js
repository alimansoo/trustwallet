import React from "react";

const todoContext = React.createContext({
    todos : [],
    add : () => {},
    toggle : () => {},
    add : () => {},
    add : () => {},
});

export default todoContext;